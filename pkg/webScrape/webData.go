package webScrape

/*import (
	"fmt"
	"reflect"
)

func ParseResponseData(responseResults map[string]interface{}, searchKey string) (interface{}, []interface{}[0].(map[string]interface{})) {
	for key, value := range responseResults{
		if key == searchKey {
			dataType := fmt.Sprintf("%v",reflect.ValueOf(value).Kind())
			if dataType == "slice" {
				return "", value.([]interface{})[0].(map[string]interface{})
			} else
			if dataType == "string" {
				return value, nil
			}
		}
	}
	err := "Not found"
	return err, nil
}*/
